<h3 class="header">Japan Times Scraper</h3>
  <div id="buttons">
    <button class="btn" id="headline-sort">Sort By Headline</button>
     
     <div>
         {{#each jtarticle}}
            <h3>{{this.title}}</h3>
            <h4>{{this.summary}}</h4>
            <h4>{{this.link}}</h4>
         {{/each}}
     </div>
    

  